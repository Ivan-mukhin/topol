// Calculation constants
export const BASE_DURABILITY = 100;
export const MAX_ITERATIONS = 10000;
export const MIN_LEVEL = 1;
export const MAX_LEVEL = 4;

// UI constants
export const FALLBACK_TTK = Infinity; // Used in leaderboard for failed calculations
export const FALLBACK_BULLETS = Infinity; // Used in leaderboard for failed calculations
export const FALLBACK_DPS = 0; // Used in leaderboard for failed calculations

// Performance constants
export const DEBOUNCE_DELAY_MS = 300; // Debounce delay for headshot ratio input

// Validation constants
export const MIN_HEADSHOT_RATIO = 0;
export const MAX_HEADSHOT_RATIO = 1;
export const MIN_DAMAGE = 0.0001; // Minimum damage to prevent division by zero
export const MIN_FIRE_RATE = 0.0001; // Minimum fire rate to prevent division by zero



